version: '3.8'
services:
  app:
    build: .
    ports:
      - "46127:46127"
    volumes:
      - ./data:/app/data
    environment:
      - JWT_SECRET=changeme-replace-with-secure-secret
      - NODE_ENV=production
      - DB_PATH=/app/data/htf.db
      - PORT=46127
    restart: unless-stopped
